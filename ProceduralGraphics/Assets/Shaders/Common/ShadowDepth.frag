// Nothing to do. There is no color attachment on the shadow framebuffer, opengl writes
// the depth on its own. A fragment shader is still required for this renderer. 
// (its  setupto require both frament and vertex shaders)
void main()
{
}